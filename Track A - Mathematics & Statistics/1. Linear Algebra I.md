# Context:

AI has evolved to generate text, write code, create images, spin music and produce videos.  

But AI neither understands human emotions nor grasps the meaning of the text / code its spewing out. So how does AI do it. 

AI as a System understands numbers. And through number manipuulation it does all the above. 

Modern AI turns things (words, sentences, images, users, products) into lists of numbers (vectors), and then does two operations on them over and over

1. measuring angles between them (similarity) and
2. transforming them (matrix multiplication).

Everything below is the vocabulary for those two moves.

# Core Concepts

## Scalars → Vectors → Matrices → Tensors

Think of these as boxes of increasing dimension. Same numbers, more structure.

**Scalar — one number.** Rank-0.
Examples: temperature = 23.5, a model's confidence = 0.87, price = ₹499.

**Vector — an ordered list of numbers.** Rank-1.
The key mental shift: a vector is simultaneously **a point** in space (coordinates) and **an arrow** from the origin to that point (direction + length). Both views matter.

*Toy example you'll reuse all week — describe movies with just 2 numbers, [action, romance]:*

```
Die Hard        = [9, 1]    ← almost pure action
The Notebook    = [1, 9]    ← almost pure romance
Mr. & Mrs. Smith = [7, 6]   ← both
```

Each movie is now a point on a 2-D map, and "similar movies" = "nearby points." That's the entire idea of embeddings — except real embedding models use 512–3072 dimensions instead of 2, and *learn* the dimensions from data instead of us naming them. A sentence embedding is exactly this: a list of ~1536 numbers that locates the sentence's *meaning* in space.

*Second example — a customer as a vector:* [avg_order_value, orders_per_month, months_since_signup] → customer_A = [1200, 3, 14]. Every ML system that "profiles" users is building vectors like this.

**Matrix — a 2-D grid of numbers.** Rank-2. Two ways you'll meet it:

1. **A stack of vectors (data).** 10 movies × 2 dimensions = a (10×2) matrix. A batch of 1,000 document embeddings at 1536 dims = a (1000×1536) matrix. Rows = items, columns = dimensions.
2. **A transformation (machine).** A neural-network layer's weights ARE a matrix — a machine that eats a vector and outputs a new vector (details in Concept 4).

**Tensor — the general n-D case.** Rank-3 and up. Just "more axes":

- A color image: (height × width × 3 RGB channels) → 3-D.
- A *batch* of images: (batch × height × width × channels) → 4-D.
- LLM activations: (batch × sequence_length × hidden_dim) — e.g., (8 requests × 2048 tokens × 4096 dims). When engineers discuss "activation memory," this object is what's filling the GPU.

**Shapes — the #1 practical skill.**
Every ML debugging conversation starts with "what's the shape?" Notation: (rows × columns).

```
(3×4) · (4×2) → (3×2)   ✓  inner numbers match (4=4), they "cancel"
(3×4) · (2×4) → ERROR   ✗  inner numbers don't match (4≠2)
```

When an engineer says "dimension mismatch," this is literally all they mean: two boxes whose edges don't line up. You'll trigger this error on purpose in the notebook so you've *seen* one.

**Row vs column vectors, transpose.**
Transpose (Aᵀ) flips rows↔columns: a (3×4) becomes (4×3). It exists mostly to make shapes line up.
Read the most famous equation in deep learning: **y = Wx + b** — "take input vector x, transform it with weight matrix W, shift it by bias vector b, get output y." A tiny concrete one:

```
Given:
  W = [[1, 0, 2],        (2×3 weight matrix)
       [0, 1, 1]]

  x = [10, 20, 30]       (input vector, 3×1)

  b = [1, 5]             (bias vector, 2×1)

Step 1 — Wx (each row of W dot x):
  row 1: 1·10 + 0·20 + 2·30 = 10 + 0 + 60 = 70
  row 2: 0·10 + 1·20 + 1·30 =  0 + 20 + 30 = 50

  Wx = [70, 50]

Step 2 — add b:
  y = Wx + b = [70+1, 50+5] = [71, 55]
```

Shapes: (2×3)·(3×1)+(2×1) → (2×1). A 3-number input became a 2-number output. That's what one neural layer does — millions of times, with learned W.

## The Dot Product

This is the most important operation in AI.

**The formula (two equivalent faces):**

`a · b = Σ aᵢbᵢ` (algebra: multiply matching positions, add up) `= |a||b|cos(θ)` (geometry: lengths × angle-alignment)

**The intuition:** the dot product is an **alignment score** between two arrows.

- Large positive → pointing the same way (similar)
- Zero → perpendicular (unrelated, "orthogonal")
- Negative → pointing in opposing directions (dissimilar)

**Worked example with the movies:**

```
Die Hard  · Mr.&Mrs.Smith = [9,1]·[7,6] = 63 + 6  = 69   ← high: both action-heavy
Die Hard  · The Notebook  = [9,1]·[1,9] = 9 + 9   = 18   ← low: different genres
```

The numbers *behave like a similarity ranking*. No magic — multiply and add.

**A physical analogy:** pushing a shopping cart. Push in the direction it faces → full effect (max dot product). Push sideways → nothing (zero). Push backwards → negative. The dot product measures "how much of your effort counts."

**Where you'll meet it in AI (three places, same operation):**

1. **Attention IS dot products** (Week 4 preview). Inside a transformer, every token asks "who's relevant to me?" by taking dot products between its Query vector and all Key vectors. GPT computing attention over a 2,000-token context = millions of dot products *per layer*.
2. **Retrieval IS dot products.** A vector DB answering "find documents similar to this query" computes query·document for candidates and ranks. Your RAG pipeline's "relevance" is this number.
3. **Recommendations ARE dot products.** user_vector · item_vector = predicted affinity. If user = [loves_action=8, loves_romance=2] then user·DieHard = 8·9+2·1 = 74, user·Notebook = 8·1+2·9 = 26. Recommend Die Hard. Netflix-style matrix factorization is this at scale.



## Cosine Similarity vs Euclidean Distance (two ways to say "close")

**Cosine similarity** = the dot product after ignoring lengths = cos(θ). Range [−1, +1].

`cos(a,b) = (a·b) / (|a||b|)`  — where |a| = √(Σaᵢ²) is the vector's length.

It answers: **"do these arrows point the same way?"** — direction only, magnitude ignored.

**Euclidean (L2) distance** = straight-line distance between the two *points*: `√(Σ(aᵢ−bᵢ)²)`. It answers: **"how far apart are they?"** — and length matters.

**Worked example (do by hand once — this is the classic):**

```
a = [1, 2, 2],  b = [2, 1, 2]
a·b = 1·2 + 2·1 + 2·2 = 8
|a| = √(1+4+4) = 3,  |b| = √(4+1+4) = 3
cosine = 8 / (3·3) = 8/9 ≈ 0.89  → highly similar
```

**When the two measures disagree (the example that makes it click):**

```
a = [2, 2]      ← a short document about "pricing"
b = [8, 8]      ← a long document about "pricing" (same topic, 4× the words/intensity)
c = [2, -2]     ← a document about something else entirely

cosine(a,b) = 1.0        (identical direction — same topic!)
L2(a,b)     = √72 ≈ 8.49 (far apart — because b is "bigger")
cosine(a,c) = 0.0        (orthogonal — unrelated)
L2(a,c)     = 4.0        (…closer than b! misleading for topic search)
```

By Euclidean distance, the unrelated document c looks *closer* to a than the same-topic document b does. This is exactly why **text search uses cosine**: you want "same topic," not "same length." Euclidean shows up where magnitude is meaningful (clustering physical measurements, some image embeddings).

**The fake-debate insurance (Principal-level detail):** if embeddings are **normalized** (rescaled to length 1 — most modern embedding APIs do this by default), then cosine ranking ≡ dot-product ranking ≡ Euclidean ranking. They give the same order. So if you hear a team debating "cosine vs dot product" over normalized vectors — the debate is empty, and you're the person who can say so.

**PM implication:** when your engineer says "we use cosine over L2," they're saying "we care about semantic direction, not vector length." Nod, and ask whether the embeddings are normalized.

## Matrix Multiplication (the engine room)

**Mechanics:** every cell of the output = one dot product (row of A · column of B).
(m×n)·(n×p) → (m×p), costing m·n·p multiply-adds.

**Worked 2×2 example (once by hand, ever):**

```
A = [[1, 2],   B = [[5, 6],    AB = [[1·5+2·7, 1·6+2·8],   = [[19, 22],     
		[3, 4]]        [7, 8]]          [3·5+4·7, 3·6+4·8]]      [43, 50]]
```

**Two mental models (both true):**

1. **Transformation:** a matrix is a machine that reshapes space — rotate, stretch, squash, shear. Multiplying a vector by a matrix moves the point somewhere new. (Watch 3Blue1Brown Ch.3 — this is THE visual.)
2. **Composition:** multiplying two matrices chains two transformations into one — "rotate, THEN stretch." **This is what a deep network is:** layer after layer of transform → the input vector (an image, a sentence) gets progressively reshaped until, in the final space, the answer is easy to read off. "Deep" = many chained transformations.

**Scale check (why this is the whole game):** GPT-class models have *billions* of weights organized as matrices. Generating ONE token = pushing vectors through hundreds of large matrix multiplications. Your API bill is, to first order, a matrix-multiplication bill.

**Why GPUs (the punchline):** each output cell of a matrix multiply is an *independent* dot product — thousands of them, none waiting on another. GPUs have thousands of small cores designed to do exactly this in parallel (they were built to transform 3-D game geometry — same math). CPUs have a few dozen powerful cores; GPUs win at this workload by 10–100×. **This single fact explains NVIDIA's position in AI** (the full origin story is this week's Track E: AlexNet).



**Identity and inverse (intuition only):**

- **Identity matrix I** = the "do nothing" transformation (1s on the diagonal): AI = A. The number 1 of the matrix world.
- **Inverse A⁻¹** = the "undo" button: A⁻¹A = I. If A doubles every vector, A⁻¹ halves it.
- Not everything can be undone: a matrix that flattens 3-D space onto a plane *destroyed* the third dimension — no inverse exists. **"Non-invertible = information was lost."** You'll never invert a matrix as a PM, but that metaphor is genuinely useful (lossy compression, dimensionality reduction, irreversible aggregations).



## Basis, Vector Spaces, Orthogonality, Linear Independence (the coordinate system)

**Vector space:** everything you can reach by adding and scaling vectors. The "world" the vectors live in.

**Basis:** the minimal set of reference directions that describes the whole space — the axes of your coordinate system.

*The analogy that lands:* **RGB color.** Every color your screen shows = some mix of (red, green, blue). Three basis directions, and (r, g, b) coordinates describe any color. An embedding model with dimension 1536 describes any *meaning* as a mix of 1536 learned directions — a color system for semantics, where nobody hand-named the axes.

**Linear independence:** no vector in your set is redundant — none can be built from the others.

*Concrete redundancy example:* features [price_usd, price_inr] are linearly *dependent* (one is a fixed multiple of the other) — the second column adds cost, zero information. [price, weight] are independent — knowing one doesn't give you the other. Redundant dimensions = paying storage and compute for nothing.

**Orthogonality:** perpendicular; dot product = 0; "fully uncorrelated directions." Latitude and longitude are orthogonal — moving north tells you nothing about east. In embedding spaces, roughly-orthogonal directions are how a model packs *many* independent concepts (tense, sentiment, topic, formality…) into one space without them interfering. (Bonus fact for the curious: in high dimensions there's room for *nearly*-orthogonal directions vastly exceeding the dimension count — one reason 1536 numbers can encode so much. Look up "superposition" — Anthropic's interpretability work — only if you enjoy rabbit holes.)

> Ref for “Superposition”: [https://transformer-circuits.pub/2022/toy_model/index.html](https://transformer-circuits.pub/2022/toy_model/index.html)

**PM implication — a real conversation this vocabulary unlocks:**

> *You:* "Vector DB cost is scaling with dimensions. Can we cut dimensions without hurting retrieval quality?"
> *Engineer:* "We can try Matryoshka embeddings — truncate 1536 → 512 — or PCA."
> *You:* "So we'd keep the most informative directions and drop near-redundant ones. What recall do we lose at 512? Can we A/B it on the golden set?"

That exchange — cost lever, technique, measured trade-off — is Principal-level, and it's just Concept 5 + Week 5's evals.

## Common Misconceptions

1. *"Embedding dimensions are human concepts like 'happiness.'"*
  1. No — they're learned, distributed, and individually meaningless; only directions/regions have meaning.
2. *"Higher cosine similarity always means better retrieval."*
  1. It means *nearer in the embedding model's opinion* — if the model wasn't trained for your domain, near can be wrong (Week 8: domain shift, rerankers).
3. *"A 3072-dim embedding is twice as good as 1536."*
  1. More dims = more capacity AND more cost; quality depends on the model, not the count.
4. *"The matrix math is what's hard about AI."*
  1. The operations are multiply-and-add, learnable in a week; the hard parts are data, evals, and product judgment — the rest of this program.



# PM Perspective

## Why this topic is important for PMs?

- **Embeddings are vectors.** Every semantic feature you ship — search, dedup, clustering, recommendations, RAG — rests on vector geometry. When quality disappoints, the geometry (embedding model choice, normalization, domain fit) is suspect #1.
- **Similarity search depends on cosine similarity.** "Retrieval brought back garbage" often decomposes into: wrong embedding model for the domain, unnormalized vectors, or chunks whose vectors blur multiple topics (Week 8).
- **Recommendation systems compare vectors.** user·item = affinity — matrix factorization, the pre-LLM workhorse that still runs large parts of the internet.
- **RAG retrieves nearby embeddings.** "The bot answered from the wrong document" often = "the nearest vectors weren't the right vectors." You can now ask *which* — the query's embedding, the docs', or the distance metric?
- **Cost conversations are shape conversations.** Tokens × dimensions × layers = compute. The vocabulary of shapes lets you follow — and interrogate — every infra cost discussion in Weeks 4–12.



## Interview Prep (Track A) — with model answers

- *What is cosine similarity and why is it preferred over Euclidean distance for text embeddings?*
  - **Model answer:** "Cosine measures the angle between two vectors — direction only — while Euclidean measures straight-line distance, which mixes in magnitude. For text, direction encodes topic/meaning and magnitude often just reflects length or intensity, so two documents about the same topic at different lengths score as similar under cosine but far apart under L2. That's why semantic search defaults to cosine." *(Bonus: for normalized embeddings the two rankings coincide.)*
- *Explain the dot product to a non-technical stakeholder.*
  - **Model answer:** "It's an alignment score between two arrows: big positive = pointing the same way, zero = unrelated, negative = opposite. Like pushing a shopping cart — only the push in the direction it faces counts."
- *Why are GPUs good at deep learning?*
  - **Model answer:** "Deep learning is overwhelmingly matrix multiplication, and every cell of a matrix multiply is an independent dot product — thousands of small jobs with no dependencies. GPUs have thousands of cores built for exactly that parallel pattern; CPUs have a few powerful cores optimized for sequential work. Same reason they were good at 3-D games — same math."
- *An engineer says the retrieval index uses "normalized embeddings with dot-product search." What does that imply?*
  - **Model answer:** "Normalized = all vectors scaled to length 1, so dot product equals cosine similarity — ranking is purely by direction and unaffected by document length. It also tells me any cosine-vs-dot-product debate for this index is moot."
- *We want to cut vector-DB costs. What are the levers on the embedding side?*
  - **Model answer:** "Reduce dimensions (Matryoshka truncation or PCA — keep the most informative directions, drop near-redundant ones), quantize the stored vectors, or shrink the corpus via dedup. Each trades recall for cost, so gate the change on retrieval metrics against a golden set rather than eyeballing it."
- *What does 'dimension mismatch' mean when an engineer says a pipeline broke?*
  - **Model answer:** "Two arrays whose shapes don't line up for multiplication — e.g., a (3×4) fed into something expecting (5×p). Usually a wiring bug: wrong embedding size, wrong batch shape, or a model swap that changed output dimensions — which is why changing embedding models mid-flight requires re-indexing."



# Practice (NumPy notebook — spec)

1_linear_algebra.ipynb (Google Colab, free). 
Expected time: 60–90 min.

1. Create vectors/matrices; inspect .shape; practice transpose and reshaping. (Checkpoint: predict each shape before running the cell.)
2. Implement dot product manually with a loop; verify against np.dot. Recreate the movie example ([9,1]·[7,6]=69) and the shopping-cart intuition in a comment.
3. Implement cosine_similarity(a, b) from scratch (dot / norms); verify against sklearn.metrics.pairwise.cosine_similarity. Reproduce the a/b/c disagreement example from Concept 3 and print both cosine and L2 side by side.
4. Matrix multiplication: verify (3×4)·(4×2)→(3×2); then trigger a dimension-mismatch error on purpose and read the message — you now recognize the most common ML error in existence. Compute the tiny y = Wx + b from Concept 1.
5. Embedding demo: use sentence-transformers (all-MiniLM-L6-v2, free, runs on CPU) to embed 10 sentences (mix 3 topics — e.g., pricing, cricket, cooking); compute the 10×10 cosine-similarity matrix; visualize with a matplotlib heatmap; confirm topics form visible blocks. (This heatmap is your lecture's money shot.)
6. Nearest-neighbor search (naive): given a query sentence ("how much does the premium plan cost?"), rank all 10 by cosine similarity — you have just built the core of RAG retrieval in ~15 lines. Note where it almost fails (a cooking sentence with the word "cost"?) — that's Week 8's opening problem.
7. Stretch: reduce embeddings to 2-D with PCA and scatter-plot the clusters — Concept 5 (keeping the most informative directions) made visible.



# Resources

## Videos (start here)

- **3Blue1Brown — Essence of Linear Algebra** (YouTube playlist, ~16 videos). The single best geometric intuition series ever made. Week 1 minimum: Ch.1 (Vectors), Ch.3 (Linear transformations & matrices), Ch.4 (Matrix multiplication), Ch.9 (Dot products). [https://www.youtube.com/playlist?list=PLZHQObOWTQDPD3MizzM2xVFitgF8hE_ab](https://www.youtube.com/playlist?list=PLZHQObOWTQDPD3MizzM2xVFitgF8hE_ab)
- **StatQuest with Josh Starmer** — Linear Algebra / word embedding videos ("Word Embedding and Word2Vec, Clearly Explained" doubles for Track E). Gentle, memorable.
- **MIT OCW 18.06 — Gilbert Strang lectures** (Lecture 1: The Geometry of Linear Equations). The classic. [https://ocw.mit.edu/courses/18-06sc-linear-algebra-fall-2011/](https://ocw.mit.edu/courses/18-06sc-linear-algebra-fall-2011/)

## Courses

- Khan Academy — Linear Algebra (free, self-paced, good for gaps).
- Imperial College London — *Mathematics for Machine Learning* specialization (Coursera; audit free) — the linear algebra course maps almost 1:1 to this module.

## Books

- **Mathematics for Machine Learning** — Deisenroth, Faisal, Ong. Free PDF: [https://mml-book.github.io](https://mml-book.github.io) — Ch.2–3 are this module. THE reference for Track A all 15 weeks.
- Gilbert Strang — *Introduction to Linear Algebra* (the standard text).
- Ivan Savov — *No Bullshit Guide to Linear Algebra* (fast, intuition-first, great for busy professionals).

## Blogs & write-ups

- BetterExplained — "An Intuitive Guide to Linear Algebra" (kalid.azad's classic intuition post).
- Simon Willison — "Embeddings: What they are and why they matter" (simonwillison.net, 2023) — the best PM-level embeddings explainer; bridges A1 → Track E Word2Vec.
- Vicki Boykis — *What are Embeddings?* (free book/PDF, vickiboykis.com) — deeper but excellent.
- Jay Alammar (jalammar.github.io) — visual explainers; bookmark the whole site now, you'll use it weekly.

## Interactive tools

- **TensorFlow Embedding Projector** — [https://projector.tensorflow.org](https://projector.tensorflow.org) — fly through Word2Vec embeddings in 3-D; screenshot-gold for your lecture and LinkedIn post.
- Google Colab (colab.research.google.com) — free notebooks, zero setup.
- GeoGebra — quick 2-D/3-D vector visualizations for whiteboard prep.

## X / Reddit

- X: @3blue1brown (Grant Sanderson), @rasbt (Sebastian Raschka — math-to-ML bridges), @svpino (practical ML threads).
- Reddit: r/learnmachinelearning (weekly "how much math do I need" threads — read the good answers), r/MachineLearning.

